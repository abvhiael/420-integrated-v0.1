{
  "name": "@420/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/sql && cp sql/*.sql dist/sql/",
    "test": "npm run build && node --test --test-concurrency=1 dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": { "node": ">=22" }
}
