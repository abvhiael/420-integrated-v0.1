import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const must=['index.html','app.js','styles.css','runtime-config.json','core/api.js','core/config.js','core/request.js','core/wallet.js'];for(const f of must)if(!fs.existsSync(path.join(root,f)))throw new Error('missing '+f);const html=fs.readFileSync(path.join(root,'index.html'),'utf8');for(const s of ['420AI','New request','Provider console','Connect wallet','app.js'])if(!html.includes(s))throw new Error('shell missing '+s);const app=fs.readFileSync(path.join(root,'app.js'),'utf8');for(const bad of ['privateKey','mnemonic','seedPhrase','eth_sendRawTransaction'])if(app.includes(bad))throw new Error('forbidden secret/raw-signing pattern '+bad);const cfg=JSON.parse(fs.readFileSync(path.join(root,'runtime-config.json'),'utf8'));if(cfg.features.writes!==false)throw new Error('writes must fail closed');console.log('420AI web static qualification PASS');
