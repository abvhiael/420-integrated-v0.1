import test from 'node:test';import assert from 'node:assert/strict';import {buildAIRequestDraft,submissionGate} from '../core/request.js';const b='0x'+'1'.repeat(64);test('builds bounded canonical request intent',()=>{const d=buildAIRequestDraft({jobId:b,modelVersionId:b,workloadClass:'TEXT',requestHash:b,privacyPolicyId:b,verificationProfileId:b,maxSpend420:'42',deadline:200},100);assert.equal(d.kind,'AIJobManager.createRequest');assert.equal(d.maxSpend420,'42');});test('rejects malformed or expired request',()=>{assert.throws(()=>buildAIRequestDraft({jobId:'x'},100));});test('submission stays fail closed until wallet adapter qualifies',()=>{assert.deepEqual(submissionGate({config:{features:{writes:false}},wallet:{account:'0xabc'},draft:{}}),{allowed:false,reason:'Wallet transaction adapter not yet qualified'});});