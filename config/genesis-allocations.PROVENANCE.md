# Genesis allocation provenance

This canonical ledger was restored into the Step 4.1 source tree from frozen Decision 13 values because
the compact Decision 18 package used as the Step 4.1 base did not carry the earlier machine-readable
allocation file. The total is verified by `420-genesis` to equal exactly 42,000,000 × 10^18 kief.
