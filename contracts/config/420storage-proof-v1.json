{
  "schema": "420-storage-proof-protocol-v1",
  "status": "PRODUCTION_QUALIFIED",
  "service": "420Store",
  "parentProtocol": "420 Resource Protocol",
  "design": {
    "proofComputation": "off-chain or specialized verifier",
    "onChainRole": "commitments, verifier policy, challenge replay protection, proof receipts and eligibility state",
    "payloadStorage": "off-chain encrypted data only",
    "proofClasses": ["REPLICA_COMMITMENT","AVAILABILITY_WINDOW","AUDIT_RESPONSE"]
  },
  "contracts": [
    "StorageIds420.sol",
    "StorageProofIds420.sol",
    "IStorageProofVerifier420.sol",
    "StorageProofSchemeRegistry420.sol",
    "StorageCapacityRegistry420.sol",
    "StorageCommitmentRegistry420.sol",
    "StorageProofRegistry420.sol",
    "StorageAgreementRegistry420.sol",
    "StorageObjectManifestRegistry420.sol",
    "StorageSettlementRegistry420.sol"
  ],
  "runtimeProfiles": {
    "developerApi": "v1",
    "topologySchema": "storage-topology-v1",
    "disasterRecoveryEvidence": "storage-dr-v1",
    "testnetEvidence": "storage-testnet-evidence-v1"
  },
  "invariants": [
    "STORAGE-PROOF-INV-001: only active 420Store nodes belonging to active staked Resource Protocol providers may originate storage commitments",
    "STORAGE-PROOF-INV-002: storage commitment identity is single-use and its provider, node, proof scheme, roots, byte size and time bounds are immutable after registration",
    "STORAGE-PROOF-INV-003: proof schemes are versioned by immutable ids and require default-deny capability authorization to register or deactivate",
    "STORAGE-PROOF-INV-004: a proof challenge must fall within the referenced storage commitment time interval",
    "STORAGE-PROOF-INV-005: a commitment/challenge pair may produce at most one accepted proof receipt",
    "STORAGE-PROOF-INV-006: proof acceptance fails closed when the store node or proof scheme is inactive, the verifier reverts, verification returns false, or the proof deadline is exceeded",
    "STORAGE-PROOF-INV-007: accepted proof bytes are not retained on-chain; only a proof digest and canonical receipt metadata are stored",
    "STORAGE-PROOF-INV-008: proof verification does not itself release payment; settlement modules consume verified proof state under separate accounting rules",
    "STORAGE-PROOF-INV-009: encrypted user payloads, erasure-coded shards and private metadata remain off-chain",
    "STORAGE-PROOF-INV-010: proof scheme verifiers are replaceable only by registering a new scheme id, preserving historical proof reproducibility",
    "STORAGE-PROOF-INV-011: storage agreements bind STORE offers, reservations, immutable commitments, proof policy and erasure parameters before activation",
    "STORAGE-PROOF-INV-012: manifests are content-immutable after sealing; repair may rotate an unavailable shard placement only while preserving shard index, shard root and byte length",
    "STORAGE-PROOF-INV-013: proof-window settlement custody remains in 420Vault and each reserved window finalizes as PAID or REFUNDED"
  ],
  "implementedExtensions": [
    "storage agreements and lease economics",
    "capacity reservations and oversubscription prevention",
    "erasure-set manifests and shard placements",
    "repair-driven placement replacement preserving content identity",
    "proof-window settlement through 420Vault",
    "developer API and SDK v1",
    "Gateway, Cache and Repair runtime integration",
    "multi-provider production topology and qualification evidence"
  ],
  "futureExtensions": [
    "random challenge scheduling through 420 Random",
    "retrieval proofs and additional bandwidth-proof profiles",
    "proof aggregation and succinct verifier adapters"
  ]
}
