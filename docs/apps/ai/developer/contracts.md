# 420 AI contracts

Genesis-compatible surfaces include `AIProviderRegistry`, `AIModelRegistry`, `AIJobManager`, `AIJobEscrow` and `AIReputationRegistry`, with mature execution routed through shared 420 ComputeMarket/Vault infrastructure.

Generated ABI/NatSpec belongs in DOC-10; integrations should bind exact service/version identities rather than hard-code undocumented assumptions.