# 420 AI events and finality

Provider/model lifecycle, job funding/matching/running/result/verification/settlement and reputation evidence produce canonical events. Indexed consumers must preserve block/finality context and reconcile reorgs.

A head observation of `SETTLED` or `VERIFIED` is still subject to the application's required chain-finality policy.