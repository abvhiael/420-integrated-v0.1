# 420 AI integration examples

For a private inference request: resolve an active model version, bind private-input storage/reference and allowed provider class, set maximum spend/deadline/verification, submit/fund, then wait for canonical match/result/verification state before accepting settlement.

For result display: show the output reference and result commitment separately from verification status; never label a commitment alone as proof of correctness.