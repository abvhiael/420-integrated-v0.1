# Analytics contracts

420 Analytics is contract-free in the Genesis profile. It observes canonical contract/protocol state through qualified indexed projections.

Analytics must not write derived scores, counts or rankings back into protocol state unless a separate protocol explicitly defines and authorizes that canonical transition; such a protocol would be outside the Analytics presentation authority.

Generated contract references belong in DOC-10.
