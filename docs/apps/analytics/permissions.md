# Analytics permissions

Ordinary Analytics use is read-only and should not request spending permissions, token approvals, Smart Account operators, reusable capabilities, session keys or recovery authority.

Saved dashboard preferences, filters or local views are client data, not protocol permissions.

If a dashboard links to a state-changing protocol action, the user must leave the observational Analytics boundary and explicitly authorize the action through the responsible application and Wallet.
