# Architecture

420 AppStore is contract-free at Genesis. It reads canonical application/service identity from 420 Registry and chain state and may consume Search, Explorer and Verify projections. Its own catalogue database, ranking, review and merchandising state is rebuildable and non-canonical.

If presentation conflicts with Registry or chain state, canonical state wins.
