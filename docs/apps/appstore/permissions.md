# Permissions

AppStore itself requires no authority to spend, sign, grant Smart Account capabilities or approve tokens. Launch/deep-link data may describe a requested action, but the Wallet must independently present and authorize it.

Installation/launch history should remain private by default unless the user explicitly opts in.
