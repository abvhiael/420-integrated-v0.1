# Arbitration integration examples

For opening a case: select the registered domain and exact origin component/object, commit claim/remedy data, then preserve the resulting case ID and snapshotted policy.

For remedy consumption: require the expected finalized case/ruling, verify chain finality and replay protection, then apply only the origin protocol's explicitly permitted state transition.