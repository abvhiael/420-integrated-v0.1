# 420 Attention integration examples

For a campaign proof: confirm active consent and campaign, verify the submitting verifier matches the campaign, ensure timestamp/window and unique nullifier, commit the proof, then reserve the deterministic reward from campaign funds.

For a reward claim: require the canonical participant or exact claim capability, release once, update paid/reserved liability accounting, and reject replay.