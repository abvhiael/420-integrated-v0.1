# Bridge APIs and route discovery

Read-only APIs may expose route availability, transfer lifecycle, proof/reconciliation context and limits. Responses must preserve network/route/asset identity and provenance.

Security-sensitive actions should re-read canonical contract state before submission, especially after provider disagreement or route changes.
