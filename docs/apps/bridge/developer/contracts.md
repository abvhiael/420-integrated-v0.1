# Bridge contracts

Important surfaces include chain, asset and route registries, gateway/router adapters, risk manager, transfer registry and accounting/reconciliation registry.

Use current Registry/deployment metadata instead of hard-coding stale addresses. Generated ABI/NatSpec belongs in DOC-10.
