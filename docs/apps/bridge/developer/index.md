# 420 Bridge developer integration

Discover current bridge services and route configuration through canonical registries. Bind integrations to explicit chain fingerprints, asset/route IDs, direction, adapter/verifier configuration and transfer IDs.

Do not treat provider proof verification as the sole authorization decision; revalidate local bridge policy and canonical state.
