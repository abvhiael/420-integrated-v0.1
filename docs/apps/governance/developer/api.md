# Governance API and reads

Expose proposal identity/class, frozen rule revision, frozen electorate snapshot, voting window, per-house tallies, action commitment, queue/timelock and execution status.

Derived APIs must not recompute or replace canonical tallies or snapshots.