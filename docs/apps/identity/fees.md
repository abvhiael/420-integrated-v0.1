# 420 Identity fees

On-chain profile, issuer and credential mutations pay ordinary network gas in native `$420`. Read-only profile/credential inspection does not mutate protocol state.

Any application-specific verification or off-chain credential service charge must be shown separately from chain gas.