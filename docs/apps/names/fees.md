# 420 Names fees

On-chain registration, renewal, updates and transfers pay normal network gas in native `$420`. If the protocol defines a separate registration/renewal economic charge, clients must present it distinctly from gas.

Read-only resolution does not mutate protocol state.