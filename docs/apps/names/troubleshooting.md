# 420 Names troubleshooting

If reveal fails, verify commitment age, salt/input consistency, availability and registration duration. If a name resolves unexpectedly, check expiry, recent updates and transfer history.

If reverse resolution is missing, confirm the address is still the active forward target and the record is unexpired.

When caches disagree, prefer canonical `Names420` state.