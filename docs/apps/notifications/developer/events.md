# Events and canonicality

Support append-only `finalized`, `retracted` and `superseded` delivery updates where appropriate. Reorg-sensitive events may change presentation before finality; finalized history is not rewritten by the notification service.
