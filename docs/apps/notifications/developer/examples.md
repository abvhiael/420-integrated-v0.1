# Examples

Read a batch from the public Indexer cursor, validate network/provenance, match private subscription policy, enqueue with a deterministic dedup key, advance the checkpoint only after successful processing, then hand any user action back to Wallet.
