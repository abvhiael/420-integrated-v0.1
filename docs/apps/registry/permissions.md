# 420 Registry permissions

Reading Registry state requires no Wallet capability. Publishing, revising or deprecating services is restricted by the Registry's authorized governance/publication rules.

A Registry entry does not grant the implementation new permissions in unrelated protocols. Consumers must still enforce the authorization model of the service they call.

Never approve a Wallet request merely because an application is listed in Registry.