# 420 Stake API and reads

Derived APIs may expose validator lifecycle, bond composition, committee targets and rewards. Security-sensitive clients should recheck canonical contract/consensus state before acting.

Expose network identity and finality context with validator projections.