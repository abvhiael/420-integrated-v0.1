# 420 Stake contracts

Primary integration surfaces include `Stake420`, `ValidatorRegistry`, `CommunityValidatorReserve` and `RewardController`.

Generated ABI/NatSpec belongs in DOC-10; this manual defines the authority and lifecycle semantics needed to use those interfaces safely.