# API

Health/status APIs should distinguish liveness from readiness and include service/network identity, freshness/cursor/checkpoint context, incident linkage and bounded diagnostic detail. Responses are observational and must not be treated as consensus or settlement proofs.
