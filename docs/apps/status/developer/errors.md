# Errors

Treat missing telemetry, stale observations, wrong-network identity and conflicting observations explicitly. Resolve conflicts against canonical consensus/execution and authoritative protocol state; do not choose whichever observation appears healthier.
