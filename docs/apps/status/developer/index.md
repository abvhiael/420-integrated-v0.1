# Developer integration

Expose role-specific liveness/readiness plus network/environment identity, freshness and evidence references. Keep status schemas stable, avoid secret-bearing telemetry and never let status consumers become protocol dependencies.
