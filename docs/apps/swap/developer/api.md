# Swap APIs and quoting

Quote APIs are read-only assistance. Responses should preserve network, asset/market/route identity, input/output amounts, fee context, expiry and provenance.

Never convert a quote response into execution without canonical revalidation and explicit Wallet authorization.
