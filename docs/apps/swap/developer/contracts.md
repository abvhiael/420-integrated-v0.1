# Swap contracts

The canonical Swap stack includes liquidity/execution and market/quote-asset registry surfaces such as the Genesis DEX factory and related approved execution components.

Use Registry/current deployment metadata rather than hard-coding stale addresses. Generated ABI/NatSpec belongs in DOC-10.
