# Swap integration examples

A safe exact-input flow is: resolve canonical services → request quote → show asset/route/fees/minimum output → request bounded Wallet authorization → submit → verify canonical receipt/finality.

For reusable capabilities, scope them to the narrowest supported asset/market/action and amount limits.
