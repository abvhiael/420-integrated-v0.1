# 420 Token concepts

**Template**: a governed token profile defining behavior available for new deployments.

**Factory deployment**: deterministic creation of a token from a selected template/configuration.

**Provenance**: creator, template/version and configuration commitment associated with the deployment.

**Creation fee**: exact 42 native `$420`, separate from gas.

**Template disablement**: blocks future factory use without rewriting already-deployed token state.
