# Token contracts

Primary integration surfaces are `TokenTemplateRegistry420` and `TokenFactory420`, plus the deployed template-specific ERC token contracts.

Generated ABI/NatSpec and machine-derived template tables belong in DOC-10. Clients should resolve current canonical addresses rather than hard-code stale deployments.
