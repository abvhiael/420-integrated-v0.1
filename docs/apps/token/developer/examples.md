# Token integration examples

A safe deployment flow is: resolve canonical factory/template registry → select enabled template → show exact behavior/configuration and 42 `$420` fee → request Wallet authorization → submit → wait for finality → record deployment provenance.

For downstream Swap/Bridge/Pay use, begin a separate qualification flow after deployment.
