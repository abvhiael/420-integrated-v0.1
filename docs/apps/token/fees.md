# 420 Token fees

Every successful TokenFactory deployment requires exactly **42 native `$420`** plus normal transaction gas.

The 42 `$420` fee is atomically forwarded to the configured community treasury. The factory should not retain the creation fee or token custody.

Third-party listing, verification, storage or application fees are separate and must not be presented as part of the TokenFactory creation fee.
