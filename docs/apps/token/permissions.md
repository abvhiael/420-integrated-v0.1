# 420 Token permissions

Token deployment requires explicit Wallet authorization for the factory call and payment of gas plus the exact creation fee.

Post-deployment owner/minter/admin permissions depend on the chosen template. Review these roles before deployment and transfer/revoke them deliberately where supported.

Factory deployment does not grant exchange, bridge or application privileges.
