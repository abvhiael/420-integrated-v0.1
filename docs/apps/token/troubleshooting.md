# 420 Token troubleshooting

If deployment fails, check Wallet balance for gas plus exactly 42 native `$420`, template enablement, configuration validity, factory identity and treasury-forwarding health.

If the new token does not appear in Swap, Bridge or another app, remember that deployment and downstream qualification are separate workflows.

When derived metadata disagrees, inspect canonical deployment/provenance and token contract state.
