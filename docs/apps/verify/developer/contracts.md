# Contracts

420 Verify has no dedicated Genesis protocol-state contract. The verification subject is deployed chain bytecode. Registry/service contracts provide identity/version context only; generated references belong in DOC-10.
