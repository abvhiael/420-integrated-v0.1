# Errors

Fail explicitly for wrong network, missing code, unsupported compiler, incomplete source bundle, unresolved libraries, code-hash mismatch or insufficient deployment metadata. Diagnostic failure must never be reported as `FULL_MATCH`.
