# Events and upgrades

A code/implementation change creates a new verification subject. Consumers should treat upgrades as new evidence events and avoid inheriting the previous implementation's result. Chain reorg/finality context still applies to deployment observations.
