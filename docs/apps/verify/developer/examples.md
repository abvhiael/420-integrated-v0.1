# Examples

Submit chain ID, contract address, complete Standard JSON source input and exact compiler/settings. Compare the compiled runtime against the deployed runtime hash, report the result class, and display proxy/implementation subjects separately when applicable.
