# Developer integration

Publish complete reproducible build inputs, bind results to network/address/code hash, and treat verification as evidence rather than endorsement. Consumers may surface results in Explorer/AppStore while preserving result class and caveats.
