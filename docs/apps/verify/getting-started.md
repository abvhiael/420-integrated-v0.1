# Getting started

Search for a deployed contract/address, confirm network and code hash, then inspect the verification result and recorded build evidence. For submissions, prefer complete Solidity Standard JSON Input with exact compiler/settings rather than flattened source.
