# Security

Never interpret `FULL_MATCH` as 'safe'. Inspect upgradeability, admin/control paths, permissions, audits and Registry identity separately. Verification evidence must preserve chain ID, address, code hash and exact build settings so results cannot be transplanted across deployments.
