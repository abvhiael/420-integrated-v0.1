# Troubleshooting

A mismatch commonly means wrong compiler/settings, missing libraries, metadata differences, constructor/immutable handling or wrong deployment. `UNVERIFIABLE` means evidence is insufficient or unsupported; do not convert it into a success badge. Verify proxy and implementation separately.
