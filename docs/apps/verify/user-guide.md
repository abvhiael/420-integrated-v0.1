# User guide

Result classes are `FULL_MATCH`, `PARTIAL_MATCH`, `MISMATCH` and `UNVERIFIABLE`. Read the evidence and caveats behind the class. For proxies, inspect the proxy shell and current implementation separately. Re-verify new implementations after upgrades.
