# CMP-1.7 — Revision-bound canonical identity preflight guard

Status: **SOURCE AND TEST COMMITTED; EXACT-HEAD QUALIFICATION PENDING; NOT MARKET ACCEPTANCE OR FOUNDATION CLOSEOUT.** CMP-1.6 resolved the currently eligible provider/node/resource ancestry, but its result is a transient view. CMP-1.7 adds `contracts/src/compute/ComputeIdentityGuard420.sol` and `contracts/test/ComputeIdentityGuard420.t.sol` to reject stale observations when subsequently compared against current canonical state.

The guard's `commitment(snapshot)` uses domain-separated standard ABI encoding of a fixed-size CMP-1.6 snapshot plus current chain ID, guard contract and all three registry addresses. It covers IDs, three revision numbers, prospective settlement account, endpoint expiry, profile and manifest commitments, class and advertised capacity. `requireCurrent(resourceId, expectedClass, requiredUnits, expected)` resolves its **own** current resource ancestry and administrative availability through CMP-1.6; it rejects zero expectations, changed revision or changed snapshot commitment and fails closed on local suspension or expiry. The caller must obtain `expected` from a trusted prior read and cannot provide arbitrary parent or beneficiary substitutes. Dedicated tests cover current ancestry, malformed/wrong-domain expectations, provider beneficiary rotation, node endpoint update, resource capacity revision and expiry. Historical revisions continue to be readable from the underlying registries.

**This is read-only preflight, not atomic match acceptance.** No identity guard call consumes a CapabilityRegistry allowance, checks signed operator enrollment, reserves capacity, freezes an economic beneficiary, verifies stake, hardware or regional claims, authorizes a scheduler, creates a match or releases funds. Callers cannot rely on a previous `requireCurrent` to authorize a future transaction: a real acceptance path must re-resolve canonical status, parentage, revisions, signed requester/provider consent, policy, capacity and actual payer-isolated Vault funding atomically in the same state-changing transaction, then persist its immutable accepted terms. The digest authenticates only the observed on-chain administrative record and is NOT a signature or objective hardware claim.

Qualification requires exact current-head Solidity Contracts, 420Docs and 420 Integrated success, with dedicated guard test execution and negative cases. CMP-1.5's outstanding independent cross-language ABI/Keccak fixtures, mutation-time scoped capabilities/delegated signing, provider verification, ProtocolRegistry publication, latest-main reconciliation and foundation closeout remain BLOCKED. CMP-0.12 funded-market and all CMP-INV-001–030 release proofs remain OPEN. No fixed Genesis predeploy or active registry publication is introduced.
