# 420 Wallet — W11.10 Closeout Status

## Repository-side qualification

W11 native implementation is repository-complete through the final release/distribution preparation slice.

- W11.1 native Android/iOS project bootstrap and bridge boundary — COMPLETE.
- W11.2 hardware-backed secure storage and session keys — COMPLETE.
- W11.3 native passkeys and biometric authorization — COMPLETE.
- W11.4 native transaction submission and RPC transport — COMPLETE.
- W11.5 verified links, QR, and dApp handoff — COMPLETE.
- W11.6 push authorization — COMPLETE.
- W11.7 Wallet / Apps / Activity / Security native UX — COMPLETE.
- W11.8 lifecycle and hostile-state hardening — COMPLETE.
- W11.9 automated native qualification — COMPLETE.
- W11.10.1 unsigned release APK/AAB and generic-device iOS archive — COMPLETE.
- W11.10.2 store/privacy/entitlement hardening — COMPLETE.
- W11.10.3 physical-device evidence gate and machine-readable evidence record — COMPLETE as repository infrastructure; real-device evidence remains external.
- W11.10.4 authorized Play/TestFlight distribution preparation — COMPLETE as repository infrastructure.

Latest qualified repository head before this closeout record: `a13ba9eeea9caf5544218ac5b7da937d1e0c9711`.

Qualification:

- 420 Wallet Mobile Verification #524 — PASS.
- 420 Integrated Qualification #1469 — PASS.

## Reconciliation state

PR #112 targets `main` and remains draft. At this checkpoint `main` is still `6c5dc85066c6af63a08f9052bc661cab21c6e86c`, the same W10 merge commit on which W11 was based. No main-branch drift or reconciliation commit is required. PR #112 remains mergeable.

## External release blocker

W11 must not be declared release-qualified from simulator/emulator results alone. The remaining work requires authorized physical Android and iOS hardware plus external signing/provisioning material.

The canonical evidence file is `device-qualification-results.json`. Each required Android and iOS row must be changed from `BLOCKED_EXTERNAL_DEVICE` to `PASS` only after the corresponding behavior is exercised on real hardware. Every physical-device result must include device model, OS version, qualified build SHA, and notes.

The physical-device matrix covers fresh install/upgrade, passkey registration/assertion, biometric/device-owner gating, session-key creation/rotation, locked-device behavior, background/foreground privacy locking, capture handling, verified links while locked/unlocked, push foreground/background/terminated/replay, stale/expired requests, account/chain/auth-epoch drift, lost-device cleanup, clipboard policy, and RPC transport.

`npm run device:closeout` is the authoritative release closeout gate. It must continue to fail until all required real-device evidence is present and passing.

## Authorized distribution requirements

Android Play internal testing requires signing material supplied outside source control through the documented `WALLET420_ANDROID_*` environment variables. Partial signing configuration fails closed. Normal CI remains unsigned.

iOS TestFlight/App Store Connect distribution requires an authorized Apple signing identity, provisioning profile, production APNs entitlement, configured Associated Domains host, and export settings supplied outside source control. The repository contains only a non-secret export-options template.

No keystore, private key, certificate, provisioning profile, APNs signing credential, recovery secret, passkey secret, or wallet signing key may be committed.

## Final W11 gate

Before PR #112 is made ready for review or merged:

1. Produce authorized signed Android and iOS builds from the W11 head.
2. Complete the physical-device evidence matrix on real hardware.
3. Run `npm run device:closeout` successfully.
4. Confirm Play internal-testing and TestFlight/export metadata and entitlements are correct.
5. Re-check `main` for drift and reconcile if needed.
6. Run 420 Wallet Mobile Verification and 420 Integrated Qualification on the final reconciled head.
7. Only then mark PR #112 ready for review/merge.

Until those external-device requirements are satisfied, PR #112 intentionally remains draft and W11.10 remains `BLOCKED — EXTERNAL PHYSICAL DEVICE / AUTHORIZED SIGNING ENVIRONMENT` rather than falsely marked complete.
